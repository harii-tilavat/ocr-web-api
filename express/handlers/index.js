// const {  } = require('./authorize');

const authorized = require("./authorize");
const logger = require("./logger");

// const { logger } = require('./logger');
module.exports = { authorized, logger }